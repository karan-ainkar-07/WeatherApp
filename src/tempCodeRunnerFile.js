    // console.log(`Current Temperature in ${city}: ${data.current.temp_c}°C`);
    // console.log(`Condition: ${data.current.condition.text}`);

    // // Air Quality Index (AQI)
    // if (data.current.air_quality) {
    //   console.log(`Air Quality PM2.5: ${data.current.air_quality.pm2_5}`);
    // } else {
    //   console.log("Air quality data not available");
    // }

    // // Forecast
    // data.forecast.forecastday.forEach(day => {
    //   console.log(`Date: ${day.date} - Max: ${day.day.maxtemp_c}°C, Min: ${day.day.mintemp_c}°C, Condition: ${day.day.condition.text}`);
    // });